import { LandingPage } from "@/components/LandingPage";

/**
 * Arabic home route — full landing page (coming-soon hidden).
 */
export default function ArabicPage() {
  return <LandingPage locale="ar" />;
}
